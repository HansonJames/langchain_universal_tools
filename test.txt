Anthropic 团队分享了三种控制任务流程的方法：
第一种就是最简单的“Think step by step”，大家应该都会用了。
第二种就是我们在提示词中制定好思考步骤，第一步干嘛，第二步干嘛，第三步干嘛……这种方法相比于第一种方法会更可控，同时也更加局限，因为它限制了大模型做其他探索的可能。
第三种方法，比较有意思。你可以用 <thinking> 和 <answer> 这样的 XML 标签去引导大模型的思考流程。

老师，能不能直接大模型连接数据库，不用输入表结构，自动生成sql语句并且执行，返回结果呢？

山特ups再出问题，插拔一下usb接口，并且按重置按钮即可。